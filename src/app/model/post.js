class Post {
  constructor(title, date, description, imageUrl, imageLabel, linkText) {
    this.title = title;
    this.date = date;
    this.description = description;
    this.imageUrl = imageUrl;
    this.imageLabel = imageLabel;
    this.linkText = linkText;
  }
}

export default Post;

class Post {
  constructor(title, date, description, imageContent, imageLabel, linkText) {
    this.title = title;
    this.date = date;
    this.description = description;
    this.imageContent = imageContent;
    this.imageLabel = imageLabel;
    this.linkText = linkText;
  }
}

export default Post;
